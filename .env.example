# Public JSON-RPC endpoint for Robinhood Chain (chain id 4663), used first by the browser.
NEXT_PUBLIC_RPC_URL=https://rpc.mainnet.chain.robinhood.com
# Optional private/Alchemy RPC used first on the server. Leave unset to use NEXT_PUBLIC_RPC_URL.
RPC_URL=
# Optional: comma-separated list of RPC endpoints the server falls through, in order. Defaults to the
# official endpoint plus public fallbacks.
RPC_URLS=

# Public origin of this deployment (used for Open Graph metadata).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: an upstream snapshot API implementing GET /v1/public/vaults.
# When unset, /api/public/vaults reads every vault directly from the chain.
UPSTREAM_API_URL=

# Server-side Robinhood Chain RPC. Never expose these to the browser.
RH_RPC_URL=
RH_ARCHIVE_RPC_URL=

# Launch: dry-run unless STOCKIFY_MAINNET_DEPLOY=YES. Signer is never committed.
STOCKIFY_MAINNET_DEPLOY=
STOCKIFY_DEPLOYER_KEY=

# Optional: Uniswap V3 QuoterV2 / SwapRouter02 on Robinhood Chain for the Trade page.
NEXT_PUBLIC_UNISWAP_QUOTER=
NEXT_PUBLIC_UNISWAP_ROUTER=

# Optional: support portal linked from /help/contact.
NEXT_PUBLIC_SUPPORT_URL=

# Optional: protocol token contract (defaults to the deployed token on Robinhood Chain).
NEXT_PUBLIC_TOKEN_ADDRESS=

# Optional: enables the written brief and the question box on /intelligence.
# Without it the page still shows the signals computed from chain reads.
ANTHROPIC_API_KEY=
# Optional: model used by the analyst (defaults to claude-opus-5).
INTELLIGENCE_MODEL=
